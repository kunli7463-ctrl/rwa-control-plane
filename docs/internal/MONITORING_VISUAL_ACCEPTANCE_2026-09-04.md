# 本机监控视觉验收

范围：原生 Grafana 13.2.1 + Prometheus + Alertmanager，本机隔离合成数据。
本次运行目录：`.local/monitoring-runtime-iVATku/`。
浏览器：Codex 应用内浏览器，1280×720；使用临时本地管理员登录，未开启匿名访问。

## 实际观察

- 登录表单、登录后主页和 RWA Worker Operations 仪表盘正常加载。
- DOM 中出现 26 个面板标题；顶部 Outbox 采集/就绪、积压/等待时长图正常渲染。
- 实际截图观察到就绪从 0 恢复至 1；积压为 0；标题、轴和图例可辨认。
- 滚动查看 Web 延迟、数据库探针、统计可见性和连接使用率，未出现查询报错提示。
- 底部主机空闲内存、应用文件系统可用比例、Web 进程常驻内存曲线实际绘制。
- 截图已在本次任务工具输出中观察；没有伪造本地 PNG 文件路径。
- 检查结束关闭本次临时标签页，运行脚本按有限窗口关闭服务并抹去临时登录文件。

## 边界

这是当前视口和代表性面板的人工视觉检查，不是全部设备/全部交互的自动化端到端测试。
合成 exporter 未持续产生业务请求，因此 Web p95 没有可用延迟样本，图表留空：
不能把空白解释为 0 延迟或健康。查询解析/数据源连通性另由原生脚本验证。
不代表生产分页通知接收、生产数据隔离、真实数据库负载或移动端布局已验收。
之前 Chrome SIGABRT、旧临时窗口过期和连接拒绝记录保留，不改写为通过。
