# RWA 产品交付状态（2026-08-27）

## 当前判定

- 内部产品代码与合成数据 PoC：完成，可用于机构技术评估和合作演示；
- PostgreSQL 全量验收：104/104 通过，0 失败，0 跳过；
- 一键准备演示数据：真实 PostgreSQL 验证通过，返回 `DEMO_READY`；
- 自动演示向导：12 步，自动切换服务器身份、突出当前操作并说明预期结果；
- 最新迁移：`017_isolated_prover_jobs.sql`。

## 本轮新增

1. 固定 `sandbox-hk / hk-liquidity-sandbox` 的合成数据重置；
2. 生产模式、错误租户、错误产品和错误 NAV Envelope 全部失败关闭；
3. 事务内恢复产品、凭证、有效签名 NAV、资产账、名册账、异常、审计和 Outbox；
4. 证明重置不改动其他 208 个本地测试产品及其 613 笔交易；
5. 12 步客户演示路径覆盖认购、转让、赎回、外部失败、双人处置、资格限制、预期拒绝和受限退出；
6. 浏览器实际验收角色自动切换和当前动作高亮。

## 完整向导后的预期状态

- 产品：`ACTIVE`；
- 资产账：90 份；
- 法定名册：90 份；
- 一致性：`MATCHED`；
- 现金事件：5/5；
- 未结异常：0。

## 边界

该功能仅用于合成数据 Sandbox。正式电路审计与 ceremony、真实 Prover/Vault/HSM、客户
OIDC/KMS、银行/托管/过户代理接口、权威根发布及香港/迪拜法律与持牌机构确认仍是生产前置项。
