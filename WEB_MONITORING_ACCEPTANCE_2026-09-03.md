# 第四阶段：Web、数据库及主机监控增量验收

日期：2026-09-03。范围：本地代码和本机原生工具，不是生产上线批准。

## 已实现

独立、默认关闭的 Web 指标监听；固定低基数请求计数和延迟直方图；PostgreSQL
只读单连接采集、超时、权限可见性标记；主机 CPU/内存与应用文件系统指标。
新增 12 条告警，总计 34 条；新增 13 个面板，总计 26 个。

## 验证证据

- `./scripts/verify-local-postgres.sh`：155 项通过，0 失败、0 跳过。
  包含真实 PostgreSQL 查询及只读会话确认。没有为监控临时授予额外数据库权限。
- `./scripts/verify-monitoring.sh`：16 项 Node 监控测试，原生 promtool 校验
  34 条规则与 40 个场景（新增正常基线、数据库恢复及各新增规则正例）。
- `scripts/verify-monitoring-runtime.js`：Prometheus、Alertmanager、Grafana 启动成功；
  26 个面板加载、匿名访问拒绝、所有面板 PromQL 原生解析成功。
  合成 exporter 故障与恢复、认证本机 webhook firing/resolved 链路通过。
- 原生运行报告：`.local/monitoring-runtime-wdDBCu/report.json`。
- 首次 HTTP 测试因沙箱禁止本机监听而失败；获得本回合网络权限后原样重跑通过，
  没有删除或跳过失败测试。

原生监控烟雾测试使用合成数据库指标；真实数据库采集由独立集成测试验证，不能将两者
表述成真实数据库故障贯穿全部告警链路的演练。Grafana 已做 API/配置验证，未做浏览器视觉验收。
临时原生监控服务测试结束后已停止。没有向外部发送通知或执行生产部署。

## 仍需完成

实际部署网络隔离及专用监控账户验收、真实值班告警确认、Grafana 浏览器视觉核验；
远程数据库磁盘/WAL/复制与容器资源限额需按最终基础设施接入。性能 SLO 应在实际
负载基线确定后设置。第五阶段仍是香港、新加坡、马来西亚、阿联酋四地产品配置；
后续仍有供应链、ZK 可复现构建和独立全仓回归，不能认定整套内部产品已全部完成。
