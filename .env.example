DATABASE_URL=postgres://rwa_app:change-me@127.0.0.1:5432/rwa_control_plane
AUTH_MODE=sandbox
COOKIE_SECURE=false
HOST=127.0.0.1
PORT=8765
ZK_MODE=disabled
# Production-only when ZK_MODE=groth16:
# ZK_ARTIFACT_DIR=/absolute/path/to/approved-groth16-bundle
# ZK_ARTIFACT_MANIFEST_SHA256=64-lowercase-hex-characters
# PROVER_MODE=isolated
# PROVER_ENDPOINT=https://prover.internal.example
# PROVER_SERVICE_TOKEN=minimum-32-character-secret
# PROVER_EXPECTED_SERVICE_ID=pinned-service-identity
# PROVER_HEALTH_PORT=8771
OUTBOX_TENANT_ID=sandbox-hk
OUTBOX_HEALTH_PORT=8770
OUTBOX_POLL_INTERVAL_MS=1000
OUTBOX_BATCH_SIZE=50
OUTBOX_LEASE_MS=30000
OUTBOX_MAX_ATTEMPTS=8
OUTBOX_BASE_DELAY_MS=1000
OUTBOX_MAX_DELAY_MS=300000
