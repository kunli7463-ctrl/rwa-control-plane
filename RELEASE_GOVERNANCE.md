# 发布证据与审批边界

1. 固定 Node 22、package-lock、候选源码和 Circom 版本，保存变更清单。
2. 运行真实 PostgreSQL 全量回归、压力/故障矩阵、监控规则与原生通知验收。
3. 运行 `scripts/build-release-evidence.js`，审查 CycloneDX 中的组件、许可证及全部漏洞报告。
   审计超时/网络失败不是没有漏洞；任何非零结果必须复核，禁止用 `audit fix --force` 自动放行。
4. 对选定生产镜像另做 OS 包 SBOM、镜像摘要固定与签名验证。当前 npm SBOM 不覆盖容器 OS。
5. 对照源码清单复核部署所用文件；哈希清单仅是完整性材料，不是签名或发布授权。
6. 完成独立只读安全复核、ZK 构建来源/ceremony 审批及数据边界验收。实现者不能自己宣称
   已完成独立审计。单次本机两份相同编译产物也不等于跨平台可复现。
7. 批准后再由明确授权的部署人执行生产发布。此工程任务不 commit/push/发布/部署。

候选配置、合成数据、测试证明/密钥一律不得混入生产证据。外部机构的事实和权威签名
由机构提供；应用方仍负责技术验签、权限、幂等、审计与失败关闭边界。

2026-09-04 最新证据为 `release-evidence/2026-09-04T16-25-04-685Z/`：生产依赖
0 项；全部依赖 15 个 Low 包条目，来自 elliptic 开发依赖链，未批准豁免。
清单完整性核验通过（168 个文件）；发布门禁仍退出 2，不能宣布发布获批。
旧查询超时记录保留，不覆盖。容器真实运行和代表性监控视觉验收现已完成，详见
`INTERNAL_CLOSEOUT_2026-09-04.md`；这不替代容器 OS 扫描、ZK 来源链和独立审计。

后续最新代码证据：`release-evidence/2026-09-05T05-21-34-916Z/`，172 文件核验通过，
生产依赖审计退出 0，全依赖退出 1；总门禁仍未关闭。镜像重验及 OS 扫描详见
`SECURITY_CLOSEOUT_FOUR_ITEMS_2026-09-04.md` 第 6 节。不得把历史镜像扫描结果
或单机测试 ZK 参数当作生产批准；独立复核尚无最终结论。

2026-09-05 驱动维护后的最新源码证据为
`release-evidence/2026-09-05T07-34-54-814Z/`（172 文件）。pg 8.23.0 全量回归通过，
生产依赖审计退出 0，全依赖退出 1，总门禁仍未关闭；旧源码清单不代表本轮版本。
